import{Component,type ErrorInfo,type ReactNode}from'react'
export class ErrorBoundary extends Component<{children:ReactNode},{error:boolean}>{state={error:false};static getDerivedStateFromError(){return{error:true}}componentDidCatch(error:Error,info:ErrorInfo){console.error('Application error',error,info)}render(){if(this.state.error)return <main className="fatal-error" role="alert"><div><h1>Evolution Field Lab needs a restart</h1><p>The simulation encountered an unexpected problem. Your saved experiment is still stored in this browser.</p><button onClick={()=>{this.setState({error:false});location.reload()}}>Retry application</button></div></main>;return this.props.children}}
